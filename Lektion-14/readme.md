# Lektion 14

Observera i westcoast-cars-starter har jag ändrat tillbaka **Vehicle** model till TypeScript's _type_ istället för _interface_.

Glöm inte att köra kommandot `npm install` när ni hämtat hem projektet.
